CREATE TABLE words (wordId INTEGER PRIMARY KEY AUTOINCREMENT, word TEXT, bookNum INTEGER, chNum INTEGER, verseNum INTEGER );
CREATE TABLE sqlite_sequence(name,seq);
CREATE TABLE `configuration` (
	`revision`	INTEGER,
	`fonts`	TEXT,
	`booknames`	TEXT,
	`title`	TEXT,
	`description`	TEXT,
	`copyrights`	TEXT,
	`sizefactor`	INTEGER
);
